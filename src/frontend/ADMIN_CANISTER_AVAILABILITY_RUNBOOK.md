# Admin Canister Availability Runbook

This document provides step-by-step instructions for checking, starting, and verifying the backend canister when the admin OTP login flow reports that the canister is unavailable or stopped.

## Prerequisites

- `dfx` CLI installed and configured
- Local replica running (for local development)
- Terminal access to the project directory

## 1. Check Canister Status

To check if the backend canister is running:

